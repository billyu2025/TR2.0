# 將本文件內兩個 server { ... } 塊複製到 nginx.conf 的 http { ... } 內部
# （與現有 listen 8000 的 server 可並存：內網繼續用 :8000，公網用 :443）
#
# 使用前請替換：
#   1) server_name 為你的域名，例如 tr.example.com www.tr.example.com;
#   2) ssl_certificate / ssl_certificate_key 為實際 PEM 路徑（見下方「證書」說明）
#   3) root 路徑若部署目錄不同，改為你的 TR UI 根目錄（反斜線改為正斜線）

# ---- HTTP：跳轉到 HTTPS（可選，建議保留）----
# server {
#     listen       80;
#     server_name  你的域名;
#     return 301 https://$host$request_uri;
# }

# ---- HTTPS：對外正式訪問 ----
# server {
#     listen       443 ssl;
#     server_name  你的域名;
#
#     ssl_certificate      C:/TR-master/TR UI/nginx-1.28.0/ssl/fullchain.pem;
#     ssl_certificate_key  C:/TR-master/TR UI/nginx-1.28.0/ssl/privkey.pem;
#
#     ssl_session_cache    shared:SSL:10m;
#     ssl_session_timeout  10m;
#     ssl_protocols        TLSv1.2 TLSv1.3;
#
#     location = /user-management.html {
#         allow 127.0.0.1;
#         allow 10.0.0.0/8;
#         allow 172.16.0.0/12;
#         allow 192.168.0.0/16;
#         deny all;
#         root "C:/TR-master/TR UI";
#     }
#
#     location / {
#         root "C:/TR-master/TR UI";
#         index login.html;
#         try_files $uri $uri/ /login.html;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";
#         proxy_connect_timeout 1800s;
#         proxy_send_timeout 1800s;
#         proxy_read_timeout 1800s;
#         proxy_buffering off;
#         proxy_request_buffering off;
#         proxy_cache off;
#     }
# }
